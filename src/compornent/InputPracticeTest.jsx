import React, { useState, useRef } from "react";

function InputPracticeTest() {
  const inputState = useRef();
  const 
  return (
    <div>
      <div>
        <input type="text" placeholder="아이디" />
        <input type="text" placeholder="패스워드" />
        <button>추가</button>
      </div>
    </div>
  );
}

export default InputPracticeTest;
